{
  "name": "donations-backend",
  "version": "1.0.0",
  "description": "An Express server that hosts a GraphQL API to manage donations.",
  "main": "dist/server.js",
  "scripts": {
    "start": "node dist/server.js",
    "dev": "concurrently \"tsc -w\" \"nodemon --watch './**/*.ts' --exec 'ts-node' server.ts\""
  },
  "author": "Brendan Dagys",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^14.3.2",
    "express": "^4.17.2",
    "express-graphql": "^0.12.0",
    "graphql": "^15.8.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "concurrently": "^7.0.0",
    "nodemon": "^2.0.15",
    "ts-node": "^10.4.0",
    "typescript": "^4.5.5"
  }
}
